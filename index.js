const head   = require('./head');
const tail   = require('./tail');
const middle = require('./middle');
const middle = require('./middle');

const head = head;
const tail = tail;
const middle = middle;

module.exports = {
  head,
  tail,
  middle
};